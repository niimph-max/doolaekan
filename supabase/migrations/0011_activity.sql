-- แท็บกิจกรรม — ออกกำลังกายและอาหาร
--
-- ไม่สร้างตารางใหม่ ใช้ตาราง records เดิมแล้วเพิ่มชนิดบันทึกเข้าไปสองแบบ
-- เพราะกติกาสิทธิ์ การแชร์เข้ากลุ่ม ชื่อคนกด และไทม์ไลน์ ทำไว้ครบแล้วที่นั่น
-- ตารางใหม่แปลว่าต้องเขียนกติกาสิทธิ์ชุดใหม่ ซึ่งเป็นจุดที่พลาดแล้วข้อมูลรั่ว
--
-- รูปร่างของ data (jsonb) ที่แอปเขียนลงไป
--   exercise : {"activity":"ยิม","minutes":45}
--   food     : {"meal":"เที่ยง","kcal":450}     ← kcal ไม่มีก็ได้ ตั้งใจให้ไม่บังคับ
--
-- รันซ้ำได้ ไม่พัง

alter type record_kind add value if not exists 'exercise';
alter type record_kind add value if not exists 'food';

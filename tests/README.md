# ชุดทดสอบด้วยเบราว์เซอร์จริง

โปรเจกต์นี้ทดสอบด้วยเบราว์เซอร์จริงเสมอ ไม่ใช่แค่ typecheck — เพราะบั๊กที่ทำให้
ผู้ใช้เสียของ (ปุ่มกดไม่ติดโดยไม่บอกเหตุผล จอสั่นตอนคีย์บอร์ดเด้ง รูปหายตอน
ปิดชีต) ไม่มีตัวไหนที่ typecheck จับได้เลยสักตัว

สคริปต์พวกนี้เคยอยู่ในโฟลเดอร์ชั่วคราวของ session ซึ่งหายไปพร้อม session
จึงย้ายเข้ามาไว้ในโปรเจกต์ ทุกตัวในนี้รันผ่านจริง ณ commit ที่เพิ่มเข้ามา

## เตรียมเซิร์ฟเวอร์ก่อน

ต้องมีสองตัวพร้อมกัน เพราะโหมดตัวอย่างกับโหมดคลาวด์เดินคนละทาง

**ก่อนอื่น: ย้าย `.env.local` ออกไปก่อน** — ถ้าไฟล์นี้อยู่ Next จะเอาค่าในนั้นมาใช้
ทับค่าที่ล้างไว้หน้าบรรทัดคำสั่ง แล้วได้แอปโหมดคลาวด์แทนโหมดตัวอย่างแบบเงียบๆ
ทดสอบจะตกทุกตัวโดยที่ไม่มีอะไรบอกว่าทำไม ย้ายไปนอกโปรเจกต์ด้วย เพราะ
`.gitignore` จับแค่ชื่อ `.env.local` เป๊ะๆ ถ้าเปลี่ยนชื่อไว้ในโปรเจกต์จะหลุดเข้า git

```bash
mv .env.local /tmp/env.local.away     # เสร็จแล้วอย่าลืมย้ายกลับ

# 4200 — โหมดตัวอย่าง (ไม่มีค่า Supabase = แอปขึ้นปุ่ม "ดูโหมดตัวอย่างก่อน")
npx next build
rm -rf /tmp/out-local && cp -r out /tmp/out-local
(cd /tmp/out-local && npx serve -l 4200 .) &

# 4201 — โหมดคลาวด์ปลอม สำหรับทดสอบทางที่ต้องล็อกอิน/ต่อเน็ต
# ค่าพวกนี้ไม่ได้ต่อไปที่ไหนจริง สคริปต์ดักคำขอทุกอันด้วย Playwright route
NEXT_PUBLIC_SUPABASE_URL='https://offline-test.supabase.co' \
NEXT_PUBLIC_SUPABASE_ANON_KEY='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlhdCI6MTcwMDAwMDAwMCwiZXhwIjoyMDAwMDAwMDAwfQ.offline-test-key' \
npx next build
rm -rf /tmp/out-cloud && cp -r out /tmp/out-cloud
(cd /tmp/out-cloud && npx serve -l 4201 .) &

mv /tmp/env.local.away .env.local     # ย้ายกลับ
```

**ตรวจก่อนรันเสมอว่าเสิร์ฟของถูกตัว** — เปิด `http://localhost:4200/` แล้วต้องเห็นปุ่ม
"ดูโหมดตัวอย่างก่อน" ถ้าเห็นหน้าใส่อีเมลแทน แปลว่ากำลังเสิร์ฟ build ผิดโหมด
รันต่อไปก็ตกทั้งชุดโดยไม่ได้บอกอะไรเลยว่าโค้ดพัง

และถ้าจะฆ่าเซิร์ฟเวอร์เก่า **อย่าใช้ `pkill -f "serve -l 4200"` ตรงๆ** — บรรทัดคำสั่ง
ของ shell ที่รัน pkill ก็มีข้อความนั้นอยู่ด้วย มันจะฆ่าตัวเองก่อนที่คำสั่งถัดไปจะได้รัน
แล้วโฟลเดอร์ที่เสิร์ฟจะค้างเป็นของเก่าโดยไม่รู้ตัว ใช้ `pkill -f "serve -l 42[0]0"` แทน

กุญแจที่เก็บใบเข้าระบบใน localStorage ของโหมดคลาวด์คือ
`sb-offline-test-auth-token` — สคริปต์ที่ต้องล็อกอินจะเขียนใบปลอมลงไปเอง

## รัน

```bash
bash tests/run.sh              # ทั้งหมด
bash tests/run.sh doc pdf      # เฉพาะที่ระบุ
node tests/visit.js            # ทีละตัว เห็นรายละเอียดครบ
```

## มีอะไรบ้าง

| สคริปต์ | พอร์ต | ทดสอบว่า |
|---|---|---|
| `activity.js` | 4200 | แท็บกิจกรรม — จดออกกำลังกาย/อาหาร/บันทึกทั่วไป แก้ ลบ กรองกลุ่ม จำน้ำหนักรายเครื่อง |
| `vaccine.js` | 4200 | ประวัติวัคซีน — ลงแบบรู้แค่ปีได้ แสดงเท่าที่รู้จริง ตั้งเป็นนัดได้ |
| `visit.js` | 4200 | ชีต "พบหมอ / เอกสาร" — ไม่บังคับใส่รูป ชื่อไม่โกหกว่ามีเอกสารทั้งที่ไม่มี |
| `doc.js` | 4200 | เก็บเอกสารหลายแผ่น ลงวันที่ย้อนหลังตามใบจริง โน้ตไม่หาย |
| `pdf.js` | 4200 | เก็บ PDF ได้ ไม่ขึ้นเป็นรูปแตก บอกขนาดไฟล์ตามจริง เตือนเมื่อไฟล์ใหญ่ |
| `btnpos.js` | 4200 | ปุ่มที่ต้องกดบ่อยไม่ขยับหนีลงไปเรื่อยๆ ตามจำนวนข้อมูล |
| `photolimit.js` | 4200 | คุมพื้นที่เก็บรูป — เอกสารต้องถูกย่อก่อนเก็บ (1600px) และยังใส่ได้หลายแผ่น ส่วนบันทึกประจำวันใส่ได้รูปเดียว |
| `delacct.js` | **4201** | ลบบัญชีพร้อมข้อมูล — ต้องพิมพ์คำยืนยันก่อน, ลบไม่สำเร็จต้องไม่ล้างเครื่อง, และต้องบอกผลตามจริงว่าลบอีเมลด้วยหรือไม่ |
| `landing.js` | 4200 | หน้าแนะนำแอปที่ /welcome/ — ไม่มีไฟล์พลาด ไม่ล้นแนวนอน ปุ่มพาไปถึงตัวแอป |
| `kicker.js` | 4200 | บรรทัดบนสุดบอกชื่อสมุดที่กำลังเปิดอยู่ ครบทุกแท็บ |
| `avatar.js` | 4200 | รูปโปรไฟล์ถูกย่อก่อนเก็บ ไม่ทำให้สำเนาในเครื่องเขียนไม่ลง |
| `authgate.js` | 4201 | หน้าเข้าระบบ — ใบหมดอายุ รหัสผิด ต้องบอกให้ตรงจุด |
| `nobook.js` | 4201 | ดึงข้อมูลไม่สำเร็จ ≠ ยังไม่มีสมุด ห้ามพาไปหน้ากรอกใหม่ |
| `keeplocal.js` | 4201 | เขียนขึ้นคลาวด์สำเร็จแต่รอบอ่านถัดมายังไม่เห็น — ของที่เพิ่งกรอกต้องไม่หาย |
| `partial.js` | 4201 | ดึงมาได้บางส่วน ต้องไม่ทำเหมือนได้ครบ |
| `quiet.js` | 4201 | พลาดจริงถึงเตือน ไม่ใช่เตือนพร่ำเพรื่อจนคนเลิกอ่าน |
| `join.js` | 4201 | เข้ากลุ่มด้วยรหัส และระดับการแชร์ |
| `photoback.js` | 4201 | รูปถูกดึงเฉพาะใบที่กดดู ไม่ดึงทุกใบตอนเปิดแอป |
| `egress.js` | 4201 | นับจำนวนคำขอตอนเปิดแอป กันไม่ให้บวมกลับมาเงียบๆ |
| `keyboard.js` | 4201 | คีย์บอร์ดเด้งแล้วหน้าไม่กระตุก ไม่มีอะไรตรึงบังช่องพิมพ์ |
| `flicker.js` | 4201 | ช่องพิมพ์ไม่ถูกสร้างใหม่ซ้ำๆ ค่าที่พิมพ์และโฟกัสไม่หลุด (`FLAKY=1` จำลองเน็ตล้มเป็นช่วงๆ) |
| `boot-flicker.js` | 4201 | ตอนเปิดแอปครั้งแรก หน้าไม่สลับไปมาหลายรอบ |

## ข้อควรรู้

- **`nobook.js` ตกเป็นครั้งคราวประมาณ 1 ใน 3 มาตั้งแต่ก่อนหน้านี้แล้ว**
  เคยพิสูจน์แบบ A/B ว่าอัตราตกเท่ากันทั้งก่อนและหลังการแก้ที่สงสัย
  ห้ามใช้ผลตกของสคริปต์นี้ตัดสินว่าโค้ดใหม่พัง ให้รันซ้ำและเทียบกับ commit ก่อนหน้า
- ชิปที่เป็น `multi={false}` จะ render เป็น `role="radio"` ไม่ใช่ `button`
  ถ้าเลือกด้วย `getByRole('button')` แล้วหาไม่เจอ ให้ใช้ `.o-chip` แทน
- Playwright จับคู่ route จากตัวที่ลงทะเบียนหลังสุดก่อน ตัวดักครอบจักรวาลต้องลงทะเบียนเป็นตัวแรก
- คลิก `.backdrop` ตรงกลางจอจะไปโดนตัวชีตแทน ต้องระบุตำแหน่งด้านบน เช่น `{ position: { x: 195, y: 30 } }`
- ภาพหน้าจอที่สคริปต์ถ่ายไว้ลงที่ `tests/shots/` ซึ่งไม่ถูกเก็บเข้า git

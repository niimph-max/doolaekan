/** เลขรุ่นของไฟล์ชุดที่เครื่องนี้กำลังรันอยู่ — ตั้งตอน build ใน GitHub Actions
 *  มีไว้ตอบคำถามว่า "แก้ไปแล้วทำไมยังเป็นเหมือนเดิม" ได้ในวินาทีเดียว
 *  ถ้าเลขไม่ตรงกับรอบล่าสุด แปลว่าเบราว์เซอร์ยังเสิร์ฟไฟล์เก่าที่แคชไว้ */
const raw = process.env.NEXT_PUBLIC_BUILD ?? '';

export const buildLabel = raw
  ? `รุ่นที่ ${raw.split('·')[0]} (${(raw.split('·')[1] ?? '').slice(0, 7)})`
  : 'รุ่นทดสอบในเครื่อง';

# Doolaekan (ดูแลกัน)

สมุดสุขภาพครอบครัว ใช้ร่วมกันหลายคน — PWA ภาษาไทย

**สิ่งสำคัญที่สุดที่ต้องเข้าใจก่อนแตะโค้ด: เจ้าของแอปกับพ่อแม่ไม่ได้อยู่บ้าน
เดียวกัน** และคนดูแลใช้เครื่องของแม่ ทุกการตัดสินใจในแอปนี้เกิดจากข้อนี้ —
ข้อมูลต้องเห็นตรงกันทุกเครื่อง ทุกคนต้องรู้ว่าใครกดอะไรไว้ และห้ามมีอะไรที่
ต้องเดินไปกดที่เครื่องอีกฝ่าย

## เป้าหมายปลายทาง

ปล่อยเป็นแอปแจกฟรีบน Play Store และ App Store (มีบัญชีนักพัฒนาทั้งสองแล้ว)
ไม่รีบ แต่ทุกการตัดสินใจไม่ควรปิดทางนี้

**ลำดับที่ตกลงกันไว้ — ห้ามข้าม**

1. ~~ย้ายไป doolaekan.com~~ **เสร็จแล้ว** — แอปอยู่ที่ https://doolaekan.com
   (`public/CNAME` อยู่ในโปรเจกต์แล้ว, custom domain ตั้งใน Settings → Pages แล้ว,
   Enforce HTTPS เปิดแล้ว) ที่อยู่เดิมบน github.io เด้งมาที่นี่ให้เอง
2. ~~แจ้งเตือน Web Push~~ **เสร็จแล้ว — ส่งถึงเครื่องจริงแล้ว** (5 ก.ย. 2569)
   ส่วนตั้งค่าอยู่ในชีตโปรไฟล์ (`src/components/PushSettings.tsx`)
   ตอนนี้เปิดใช้ที่เครื่องเจ้าของแล้วหนึ่งเครื่อง **เตี่ยกับแม่ต้องไปกดเปิดเองที่
   เครื่องตัวเอง** เบราว์เซอร์ไม่ให้สมัครแทนกัน
3. **ขึ้น App Store ก่อน** ด้วย Capacitor — เจ้าของเคยทำมาแล้วกับโปรเจกต์อื่น
   และเบื่อเงื่อนไข "ต้องมีผู้ทดสอบ 12 คน 14 วัน" ของ Play Store
   (เคยเขียนไว้กลับกัน เพราะแนะนำโดยไม่ได้ถามว่าเคยทำมาก่อนไหม — จดไว้เตือนใจ)
   โปรเจกต์ iOS อยู่ที่ `ios/` แล้ว เปิดด้วย Xcode ได้เลย ส่วน Play Store ไว้ทีหลัง

เครื่องเตี่ย แม่ และน้องชายเป็น Android ทั้งหมด (เปิดแจ้งเตือนจาก Chrome ได้เลย
ไม่ต้องเพิ่มลงหน้าจอหลักก่อนเหมือน iPhone) แต่ต้องไปกดที่เครื่องเขาเอง
**ของที่เข้าคิวไว้ตอนยังไม่ได้เปิด จะไม่ย้อนกลับมาส่งทีหลัง** — ระบบปิดงานให้พร้อม
เหตุผลว่ายังไม่มีเครื่องรับ กดเปิดวันไหนก็ได้รับเฉพาะเรื่องที่เกิดหลังจากนั้น

**ต้องมีก่อนเปิดให้คนนอกใช้**

- ~~ปุ่มลบบัญชีพร้อมข้อมูลทั้งหมด~~ **ทำแล้ว** อยู่ในชีตโปรไฟล์ ใต้ปุ่มออกจากระบบ
  ลบแถว `profiles` ของตัวเองแถวเดียว ที่เหลือหายตาม cascade — ไม่ต้องรัน SQL
  แต่ถ้าอยากให้ลบตัวอีเมลที่ใช้เข้าระบบด้วย ต้องรัน `0012_delete_account.sql`
  (ยังไม่ได้รัน — แอปบอกผู้ใช้ตามจริงอยู่แล้วว่าอีเมลยังอยู่)
- ~~หน้านโยบายความเป็นส่วนตัว~~ **ทำแล้ว** ที่ `/privacy` ลิงก์จากชีตโปรไฟล์
  และจากท้ายหน้าแนะนำ — ยังต้องกรอกแบบฟอร์มประกาศว่าเป็นแอปสุขภาพตอนส่งขึ้นสโตร์
- ~~ย้ายอีเมล OTP ออกจาก Gmail ส่วนตัว~~ **ตัดสินใจแล้วว่าใช้ Gmail ต่อ**
  (12 ก.ย. 2569) เพราะคนใช้จริงยังน้อย และการย้ายมีขั้นตอนเยอะเกินความจำเป็น
  ตอนนี้ — **ห้ามหยิบเรื่องนี้มาทวงซ้ำ** เว้นแต่เจอเงื่อนไขข้างล่าง
  ขั้นตอนกับเทมเพลตภาษาไทยเก็บไว้ครบแล้วที่ `supabase/email/` หยิบมาใช้ได้ทันที
  **วันที่ต้องกลับมาดูใหม่** คือวันที่มีคนแปลกหน้าโหลดจากสโตร์จริง เพราะตอนนั้น
  เหตุผลจะเปลี่ยนจากเรื่องโควตาวันละ ~500 ฉบับ ไปเป็นเรื่องที่คนแปลกหน้าเห็น
  อีเมลส่วนตัวของเจ้าของเป็นผู้ส่ง — ซึ่งส่งไปแล้วเอาคืนไม่ได้
- ~~app password ที่หลุดในภาพหน้าจอ~~ **เพิกถอนแล้ว** (12 ก.ย. 2569) สร้างตัวใหม่
  และวางใน Supabase → SMTP Settings เรียบร้อย ทดสอบส่งรหัสผ่านแล้ว
  วิธีที่ใช้: **สร้างตัวใหม่ก่อน แล้วค่อยลบตัวเก่า** จะได้ไม่มีช่วงที่อีเมลส่งไม่ออก
  (Google ให้มีหลายตัวพร้อมกันได้) — และการเปลี่ยนรหัสนี้ไม่ได้ทำให้ใครหลุดจากระบบ
  ใบเข้าระบบในเครื่องทุกคนต่ออายุกับ Supabase ไม่เกี่ยวกับ Gmail
- ตัดสินใจเรื่องค่า Supabase — แพลนฟรี (5 GB egress / 500 MB DB / 1 GB ไฟล์)
  รับคนแปลกหน้าที่ถ่ายรูปเอกสารกันไม่ไหว และจะเต็มแบบเงียบๆ

## แอปบน iOS (Capacitor)

`npm run cap:sync` = build เว็บแล้วคัดลอกเข้าโปรเจกต์ iOS · `npm run cap:open` เปิด Xcode

**ไฟล์เว็บถูกฝังไปในตัวแอป ไม่ใช่ชี้ `server.url` ออกไปที่ doolaekan.com** — ชี้ออก
ข้างนอกคือทางที่แอปเปิลตีกลับ (กติกา 4.2 แอปที่เป็นแค่เปลือกห่อเว็บ) และเน็ตหลุด
เมื่อไหร่แอปก็เปิดไม่ขึ้น ราคาที่จ่ายคือแก้เว็บแล้วแอปไม่ได้ตาม ต้องส่งรุ่นใหม่ขึ้นสโตร์

**build ขึ้นสโตร์ด้วย Codemagic ไม่ต้องมีเครื่อง Mac** — `codemagic.yaml` อยู่ในโปรเจกต์
แล้ว เจ้าของเคยใช้วิธีนี้กับโปรเจกต์อื่นมาแล้ว (เคยเขียนไว้ว่า "ต้องมี Mac" ซึ่งผิด
— จดไว้เตือนใจว่าอย่าเดาข้อจำกัดของคนที่เคยทำมาก่อน) ตัว workflow ตั้งไม่ให้ยิงเอง
ตอน push ต้องกดสั่งเมื่อพร้อมส่งจริง และมีด่านตรวจว่า index.html ชี้ `/_next/` ถูกที่
แบบเดียวกับฝั่งเว็บ เพราะพลาดตรงนี้แอปจะค้างที่หน้ารอโดยไม่มี error อะไรเลย

**กล้อง** — ปุ่ม "ถ่ายรูป" ทุกที่ในแอปใช้กล้องของเครื่องจริง (`src/lib/camera.ts`)
ส่วนในเบราว์เซอร์ยังใช้ `<input type="file" capture>` เหมือนเดิม ทั้งหกจุดที่รับรูป
ถูกรวบให้ไปจบที่ฟังก์ชันเดียวที่รับ data URL แล้ว สองเส้นทางจึงใช้โค้ดย่อรูปและ
กติกาจำนวนรูปชุดเดียวกัน ไม่ต้องเขียนซ้ำสองที่แล้วลืมแก้ที่ใดที่หนึ่ง
ปุ่ม "เลือกรูปที่มีอยู่" **ตั้งใจไม่เปลี่ยน** เพราะช่องเลือกไฟล์ใน WKWebView เปิด
คลังไฟล์ของ iOS ได้อยู่แล้ว และเป็นทางเดียวที่ยังหยิบ PDF จากโรงพยาบาลได้
กดยกเลิกกล้องต้องเงียบ ไม่ใช่ขึ้นแถบแดง — ปลั๊กอินใช้ error ตัวเดียวกันทั้งกรณี
ยกเลิกและกรณีพังจริง ต้องแยกจากข้อความ

**แจ้งเตือนเดินคนละเส้นทางกันสองฝั่ง และห้ามให้ฝั่งหนึ่งดับอีกฝั่ง**
- เบราว์เซอร์ → Web Push ผ่าน service worker (กุญแจ VAPID)
- แอป iOS → APNs ผ่าน device token (กุญแจ .p8) เพราะ WKWebView ไม่มี service worker
- ตาราง `push_subscriptions` มีคอลัมน์ `kind` แยกสองแบบ **ตั้งใจไม่แยกตาราง**
  เพราะคำถามที่ต้องตอบคือ "คนนี้มีเครื่องไหนรับได้บ้าง" ถ้าแยกตาราง วันหนึ่ง
  จะมีที่ที่ลืมถามอีกตาราง แล้วแจ้งเตือนจะหายเงียบเฉพาะเครื่องบางชนิด
- Edge Function เช็คกุญแจของแต่ละทางแยกกัน ขาดทางไหนก็ส่งได้อีกทาง และเหตุผล
  ไปโผล่ที่ `last_error` ของข้อความนั้น ไม่ใช่ล้มทั้งฟังก์ชัน
- ส่วนที่พลาดแล้วหาสาเหตุยากที่สุดคือใบผ่าน JWT ของ APNs (ตอบกลับแค่ 403
  InvalidProviderToken) จึงแยกตรรกะการเซ็นไว้ที่ `jwt.ts` ที่ไม่แตะ Deno API
  เพื่อให้ `tests/apns.js` รันทดสอบด้วย node ได้จริง
- `APNS_ENV` ผิด (sandbox/production สลับกัน) = `BadDeviceToken` ทั้งที่โค้ดถูกหมด
  ตอนรันจาก Xcode ใส่เครื่องเองคือ sandbox — **จุดที่คนติดกันมากที่สุด** และห้ามลบ
  แถวเครื่องทิ้งเมื่อเจอ error นี้ เพราะไม่ใช่เครื่องตาย เป็นการตั้งค่าผิด

## กติกาที่ห้ามฝ่าฝืน

**ห้ามใช้ `upsert` กับตาราง `books`** — `INSERT ... ON CONFLICT DO UPDATE` ทำให้
Postgres ตรวจกติกา UPDATE ด้วยเสมอ แม้แถวนั้นยังไม่มีอยู่จริง กติกา update ของ
สมุดคือ "ต้องเข้าถึงสมุดเล่มนี้ได้ระดับ full" ซึ่งเป็นไปไม่ได้สำหรับสมุดที่กำลัง
จะถูกสร้าง ผลคือสร้างสมุดใหม่ไม่ได้เลยและได้ error 42501 ที่ชี้ไปผิดทาง
กติกาเดียวกันใช้กับทุกตารางที่ policy ของ UPDATE ต่างจาก INSERT

**ห้ามเก็บ base64 ลงคอลัมน์ text ของ Postgres** — เคยกิน egress ไป 32 GB เพราะ
รูปถุงยาถูกเขียนลง `medications.photo_path` รูปต้องขึ้น Supabase Storage เท่านั้น

**รูปคือของหนักที่สุดในแอปนี้** — ย่อก่อนเก็บเสมอ และโหลดเฉพาะใบที่ผู้ใช้กดดู
ห้ามดึงรูปทุกใบตอนเปิดแอป การไม่ดึงรูปคือเหตุผลหลักที่แอปเปิดได้เร็ว
เมื่อเปิดให้คนนอกใช้ ทุกไบต์กลายเป็นเงินที่เจ้าของจ่ายแทนคนอื่น

ค่าย่อรูปอยู่ที่ `src/lib/photo.ts` ที่เดียว มีสองระดับเพราะใช้คนละงาน
- `shrinkPhoto` 1000px/0.72 — รูปอาหาร รูปในยิม มีไว้ให้จำได้ว่าทำอะไร
- `shrinkDoc` 1600px/0.8 — รูปเอกสาร ต้องอ่านตัวเลขบนใบผลเลือดออก
- PDF ย่อไม่ได้ เก็บตามเดิม และเป็นต้นฉบับจริงจากโรงพยาบาลอยู่แล้ว

**บันทึกประจำวัน (กิจกรรม/อาหาร) ใส่ได้รูปเดียว ส่วนเอกสารใส่ได้หลายแผ่น**
เพราะผลตรวจใบเดียวมักมีหลายหน้าจริงๆ การบังคับให้เหลือแผ่นเดียวไม่ได้ประหยัด
พื้นที่ — คนจะสร้างบันทึกเพิ่มแทน จำนวนรูปเท่าเดิม ได้แค่ความรำคาญ

**ห้ามทำอะไรที่บังคับให้ทุกเครื่องล็อกอินใหม่ โดยไม่ได้รับคำสั่งชัดเจน** —
เปลี่ยนโดเมนคือตัวอย่าง เคยเผลอ deploy ไปแล้วต้องรีบย้อนกลับ

**แอปต้องไม่โกหก** — นี่เป็นหลักที่ยึดมาตลอด
- ยอดแคลอรี่ที่กรอกไม่ครบ ต้องบอกว่ามาจากกี่รายการ ไม่ใช่แสดงเป็นยอดเต็ม
- "ยังไม่ได้กดกินยา" ไม่เท่ากับ "ยังไม่ได้กินยา" — อย่าเอาไปแสดงเป็นเรื่องน่ากังวล
- วันที่ฉีดวัคซีนที่จำได้แค่ปี ต้องแสดงแค่ปี ไม่เติมวันให้ดูแม่นเกินจริง
- ดึงข้อมูลไม่สำเร็จ ไม่เท่ากับ ยังไม่มีสมุด — ห้ามพาผู้ใช้ไปหน้ากรอกข้อมูลใหม่
- ข้อความ error ต้องบอกว่าพังตรงไหนจริงๆ ไม่ใช่ข้อความรวมๆ ที่ชี้ผิดทาง

**ของที่ผู้ใช้พิมพ์หรือถ่ายไว้ ห้ามหายเพราะกดพลาด** — เคยทำผู้ใช้เสียรูปเอกสาร
19 ใบเพราะปุ่มบันทึกกดไม่ได้โดยไม่บอกเหตุผล ปิดชีตต้องเก็บของไว้ ลบต้องถามก่อน
และห้ามบังคับกรอกช่องที่ไม่จำเป็นก่อนถึงจะบันทึกได้

**ผู้สูงอายุจำอะไรไม่ครบเป็นเรื่องปกติ** — ยอมให้กรอกอายุแทนวันเกิด ยอมให้ลง
วัคซีนแบบรู้แค่ปี บังคับให้กรอกเป๊ะ = คนเดามั่วหรือไม่กรอกเลย

**อย่าเขียนอะไรที่สมมติว่ามีแค่ไม่กี่คนในกลุ่มเดียว** — แก้ทีหลังแพงที่สุด

**ห้ามตรึง `position: fixed` ครอบหน้าที่มีช่องพิมพ์** — กล่องที่ตรึงยึดกับกรอบ
หน้าเว็บ ซึ่งบนมือถือไม่หดตอนคีย์บอร์ดเด้ง เบราว์เซอร์จะพยายามเลื่อนช่องที่กำลัง
พิมพ์ให้เห็นแล้วไปสู้กับกล่องที่ตรึง กลายเป็นจอกระตุกและแตะไม่โดน ผู้ใช้ใหม่สอง
คนเจออาการนี้ตอนกรอกสมุดเล่มแรก และไม่มีใครในบ้านเจอเลยเพราะผ่านหน้านั้นไปนานแล้ว
ถ้าต้องเต็มจอ ใช้ `min-height: 100dvh` แบบไหลตามปกติ หรือ `max-height: 88dvh`
แบบชีต ซึ่งหดตามคีย์บอร์ด และอย่าให้ปุ่มอะไรลอยตรึงอยู่ล่างจอในหน้าที่ต้องพิมพ์
เพราะมันจะไปเกาะขอบคีย์บอร์ดแล้วบังช่องพอดี

## โครงสร้าง

Next.js App Router + React + TypeScript strict, `output: 'export'`
ขึ้น GitHub Pages ด้วย GitHub Actions (deploy เมื่อ push เข้า branch ที่กำหนดใน
workflow) หลังบ้าน Supabase — Postgres + RLS + Realtime + Storage + OTP ทางอีเมล

- `src/lib/store.tsx` — สถานะทั้งแอปและ action ทุกตัว เขียนลงเครื่องก่อนแล้วค่อย
  ส่งขึ้นคลาวด์ ส่งไม่สำเร็จต้องขึ้นแถบเตือนค้างไว้ ไม่ใช่ toast ที่หายไปเอง
- `src/lib/remote.ts` — คุยกับ Supabase ที่เดียว ไทม์ไลน์สุขภาพกับบันทึกประจำวัน
  ดึงคนละโควตากัน ไม่งั้นการจดทุกวันจะเบียดของเก่าหายไปเงียบๆ
- `src/lib/selectors.ts` — คำนวณทุกอย่างจาก state ไม่เก็บค่าซ้ำ
- `supabase/migrations/` — รันเรียงเลข ต้องรันซ้ำได้โดยไม่พัง

**ตาราง `records` มีคอลัมน์ `data` เป็น jsonb** — ใช้เก็บของใหม่ได้โดยไม่ต้องแก้
โครงฐานข้อมูล (วัคซีนเก็บแบบนี้) เหมาะมากเวลาที่เจ้าของยังไม่สะดวกรัน SQL

## การทดสอบ

ทดสอบด้วยเบราว์เซอร์จริงเสมอ ไม่ใช่แค่ typecheck — ชุดทดสอบ Playwright อยู่ที่
`tests/` วิธีตั้งเซิร์ฟเวอร์และรายละเอียดของแต่ละสคริปต์อยู่ใน `tests/README.md`

ก่อน push ทุกครั้ง: typecheck ผ่าน + ชุดทดสอบของฟีเจอร์นั้นผ่าน + ชุดเดิมไม่พัง
ถ้าสคริปต์เดิมตก ให้พิสูจน์ก่อนว่าตกเพราะของใหม่จริงหรือตกอยู่แล้ว (build จาก
commit ก่อนหน้าแล้วรันเทียบ) — `tests/nobook.js` ตกเป็นครั้งคราวประมาณ 1 ใน 3
มาแต่ไหนแต่ไร ห้ามใช้ผลตกของตัวนี้ตัดสินอะไร

## เรื่องที่ยังไม่เคยทดสอบ

- ระดับแชร์ **"เฉพาะวันนัด"** — ไม่เคยมีใครใช้จริงในแอป (แต่โค้ดแจ้งเตือน
  ทดสอบระดับนี้ไว้แล้วบน Postgres จริง)
- แจ้งเตือนที่ยิงจาก **ทริกเกอร์จริง** ยังไม่เคยเกิด — ที่พิสูจน์แล้วคือเส้นทาง
  ส่ง (เข้าคิว → cron → Edge Function → ขึ้นจอมือถือ) ส่วนความดันสูงเกินเกณฑ์
  กับเตือนวันนัด ยังไม่เคยมีเหตุการณ์จริงมากระตุ้น
- ยังไม่เคยเปิดแจ้งเตือนบนไอโฟน/ไอแพด (ต้องเปิดจากไอคอนหน้าจอหลักเท่านั้น)

## วันที่ย้ายโดเมน — บทเรียนที่แพงที่สุด

ย้ายไป doolaekan.com ใช้เวลาทั้งวันทั้งที่ควรจบใน 15 นาที ต้นเหตุคือสูตรบรรทัดเดียว
ใน workflow ที่เขียนไว้ก่อนหน้า

    NEXT_PUBLIC_BASE_PATH: ${{ env.CUSTOM_DOMAIN != '' && '' || format(...) }}

เป็นกับดักของ GitHub Actions ที่เขียนเลียนแบบสามทาง — ค่ากลางเป็นค่าว่างซึ่งนับ
เป็นเท็จ สูตรจึงตกไปใช้ค่าหลังสุดเสมอ **ไม่เคยคืนค่าว่างเลยแม้แต่ครั้งเดียว**
ทุก build ที่มี CNAME จึงยังชี้ไป `/doolaekan/_next/...` ขณะที่ไฟล์อยู่ที่รากโดเมน
ไฟล์ตอบ 404 หมด React ไม่เริ่มทำงาน

**อาการหลอกตามาก** หน้าเว็บที่ค้างคือ splash ที่ถูกสร้างไว้ล่วงหน้าในไฟล์ ไม่ใช่
แอปที่กำลังโหลด และเพราะ React ไม่เคยทำงาน ตัวจับเวลา 6 วินาทีที่จะเปลี่ยนข้อความ
เป็น "ใช้เวลานานกว่าปกติ" ก็ไม่เคยเริ่ม จึงดูเหมือนแค่กำลังโหลดอยู่ ไม่มี error เลย

**บทเรียน**
- ค่าที่ผิดถูกพิมพ์อยู่ในล็อกของ build ตั้งแต่รอบแรก แต่ไม่ได้เปิดดู เพราะเชื่อ
  คอมเมนต์ที่เขียนกำกับไว้ข้างสูตร **คอมเมนต์ไม่ใช่หลักฐาน ล็อกคือหลักฐาน**
- เวลาเว็บพัง ให้ดู Console ของเบราว์เซอร์เป็นอย่างแรก ไม่ใช่อย่างสุดท้าย
  ที่อยู่ของไฟล์ที่ 404 บอกคำตอบทั้งหมดในบรรทัดเดียว
- ห้ามใช้ `A && '' || B` ใน GitHub Actions เด็ดขาด คำนวณด้วย shell แล้วเขียนลง
  `$GITHUB_ENV` แทน
- ตอนนี้มีด่านตรวจใน workflow แล้ว ถ้า index.html ชี้ไฟล์ไม่ตรงกับ basePath
  build จะพังทันทีและไม่ deploy — ห้ามถอดออก

**เรื่องเบราว์เซอร์ที่ต้องรู้ตอนย้ายโดเมน**
- ตอนผูก custom domain GitHub สั่งเบราว์เซอร์เด้งจาก github.io ไปโดเมนใหม่
  แบบถาวร และ **เบราว์เซอร์จำไว้แม้จะลบ custom domain ออกแล้ว** ถอยกลับที่อยู่เดิม
  จึงไม่ได้ผลกับเครื่องที่เคยเปิด นอกจากล้างข้อมูลเว็บไซต์ทีละเครื่อง
- service worker ที่เก็บหน้าเก่าไว้ทำให้ติดกับดัก: โค้ดที่อัปเดต service worker
  อยู่ในตัวแอป พอแอปเริ่มทำงานไม่ได้ ตัวเก่าก็ค้างตลอด ต้องล้างด้วยมือ
  **แก้แล้ว** — มีตัวกู้ฝังอยู่ใน HTML ที่สร้างไว้ล่วงหน้า (`bootRescue` ใน
  `src/app/layout.tsx`) ครบ 8 วินาทีแล้วแอปยังไม่เริ่ม จะล้าง service worker
  กับแคชแล้วโหลดใหม่ให้เอง กู้ได้ครั้งเดียวต่อการเปิดหนึ่งครั้ง กู้แล้วยังไม่ขึ้น
  จะขึ้นหน้าบอกตามตรงว่าไม่ได้ติดที่ของเก่า ไม่ใช่วนล้างวนโหลดให้ดูเหมือนกำลังโหลด
  **ต้องอยู่ในไฟล์ HTML เท่านั้น ห้ามย้ายเข้าไปในโค้ด React** เพราะเคสที่ต้องกู้
  คือ "ก้อน JavaScript โหลดไม่ได้" — ตัวกู้ที่อยู่ในก้อนนั้นจะไม่มีวันได้ทำงาน
  ตัวชี้ว่าแอปเริ่มแล้วคือ `AppStarted.tsx` และป้าย `data-boot-splash` บนหน้ารอ
  (ห้ามให้ตัวกู้ตัดสินจากข้อความบนจอ เพราะ `body.textContent` รวมข้อความในแท็ก
  script ด้วย มันจะเจอคำที่ตัวเองเขียนไว้แล้วนึกว่าแอปค้างตลอดเวลา)
- อย่าถอยกลับกลางทางถ้ายังไม่รู้สาเหตุ การ build สลับไปมาสองแบบในชั่วโมงเดียว
  ทำให้แยกไม่ออกว่าอะไรเป็นผลของอะไร และทำให้เสียเวลาไปหลายชั่วโมง

## วันที่เปิดแจ้งเตือน — สองด่านที่ไม่มีอยู่ในโค้ด

ตั้งค่าจนครบทุกอย่างแล้วยังเงียบ เพราะติดสองเรื่องที่ไม่ได้อยู่ในไฟล์ไหนของโปรเจกต์เลย

**1. Supabase บังคับ JWT ที่หน้าประตู Edge Function** — ฐานข้อมูลยิงมาด้วย header
`x-notify-secret` ตามที่ออกแบบไว้ แต่โดนปัดตกก่อนถึงโค้ดของเรา
ได้ `401 UNAUTHORIZED_NO_AUTH_HEADER` **ต้องปิด "Verify JWT" ของฟังก์ชัน `notify`**
(Edge Functions → notify → Settings) ปิดแล้วยังปลอดภัย เพราะด่านจริงคือ
`x-notify-secret` ที่อยู่ในโค้ด ใครยิงมาโดยไม่มีค่าที่ตรงกันก็โดน 401 อยู่ดี

**2. deploy โค้ดฟังก์ชัน ≠ ใส่ secret ให้ฟังก์ชัน** — เป็นคนละหน้าคนละที่กัน
ลืมข้อนี้จะได้ `503 ยังไม่ได้ตั้ง secret VAPID_PUBLIC_KEY`

**สิ่งที่ช่วยให้ไล่จบใน 15 นาทีแทนที่จะเป็นทั้งวัน** คือทุกด่านตอบกลับมาเป็น
ข้อความที่บอกว่าขาดอะไร ไม่ใช่เงียบหรือขึ้น error รวมๆ ดูได้จาก
`select id, status_code, content from net._http_response order by id desc limit 3;`
ซึ่งเก็บคำตอบจริงของทุกครั้งที่ฐานข้อมูลยิงออกไป — **ที่แรกที่ควรดูเสมอเวลาแจ้งเตือนเงียบ**

## ผลตรวจความปลอดภัย (12 ก.ย. 2569)

**กลุ่มคือกุญแจของทุกอย่างในแอปนี้** — เข้ากลุ่มได้เมื่อไหร่ ก็เห็นทุกสมุดที่กลุ่ม
นั้นแชร์ไว้ ทุกด่านจึงต้องอยู่ตรงทางเข้ากลุ่ม ไม่ใช่ที่ตารางข้อมูลรายตัว

**แก้แล้วใน `0015_group_join_hardening.sql` (ต้องรันถึงจะมีผล)**
- รหัสเชิญเดิมเป็นเลขสี่หลัก = 9,000 แบบ และเรียก `join_group_by_code` ได้ไม่จำกัด
  ครั้ง ใครสมัครบัญชีใหม่แล้วไล่เดาก็เข้ากลุ่มครอบครัวที่ไม่รู้จักได้
  ตอนนี้รหัสยาว 8 ตัวจากชุดอักษร 31 ตัว (`inviteCode()` ใน `format.ts`)
  ฐานข้อมูลบังคับความยาวขั้นต่ำ และจำกัดการเดาไว้ 10 ครั้งต่อชั่วโมงต่อบัญชี
- policy `group_members_self` เดิมตรวจแค่ว่าแถวเป็นของตัวเองไหม **ไม่ได้ตรวจว่า
  กลุ่มไหน** ใครรู้ id ของกลุ่มก็เพิ่มตัวเองเข้าไปตรงๆ ได้โดยไม่ต้องมีรหัส
  เคสจริงที่สุดคือคนที่ถูกเอาออกจากกลุ่มแล้ว แต่ยังรู้ id จากเครื่องตัวเอง
  ตอนนี้เพิ่มตรงๆ ได้เฉพาะเจ้าของกลุ่ม (ตอนสร้าง) ที่เหลือต้องผ่าน RPC เท่านั้น

**ยังไม่ได้แก้ — ต้องตัดสินใจก่อน**
- ระดับ "แชร์บางส่วน" (appointments) ยังอ่านแถว `books` ได้ทั้งแถว ซึ่งมีชื่อจริง
  ที่อยู่ วันเกิด กรุ๊ปเลือด แพ้ยา และเบอร์ติดต่อลูก — `books_select` ใช้ระดับ
  `'appointments'` ส่วนข้อมูลอื่น (ยา บันทึก ความดัน) กันไว้ที่ `'full'` ถูกแล้ว
  ถ้าจะปิดต้องแยกคอลัมน์ที่อ่อนไหวออกไป หรือให้ระดับนี้อ่านผ่านฟังก์ชันที่คืน
  เฉพาะชื่อเรียก ซึ่งกระทบทั้งฝั่งแอปด้วย

**ตรวจแล้วไม่พบปัญหา** — ประวัติ git ไม่มีความลับหลุด (`.env.local` ไม่เคยถูก commit)
`records`/`medications`/`med_logs`/`watch_rules` ต้องระดับ full ครบทุกตัว
`books_update` มีทริกเกอร์กันเปลี่ยนเจ้าของ และ Storage ผูกสิทธิ์กับโฟลเดอร์ = book_id

**ข้อจำกัดของการตรวจรอบนี้** — อ่านจากโค้ดกับ SQL ทั้งหมด **ยังไม่ได้ยิงทดสอบจริง
บนฐานข้อมูลของโปรเจกต์** ตามกติกาข้างล่างเรื่อง RLS จึงยังไม่ถือว่าพิสูจน์แล้ว
จนกว่าจะรัน 0015 แล้วลองเข้ากลุ่มด้วยบัญชีที่ไม่ได้รับเชิญดูจริงๆ

## บทเรียนเรื่องการตรวจโค้ด

ตอนแก้ช่องโหว่ในโค้ดแจ้งเตือน วิธีแก้แรกที่ "อ่านแล้วดูถูก" ใช้ไม่ได้จริง —
ลบแถวของบัญชีอื่นก่อนแล้วค่อยเพิ่มใหม่ ฟังดูตรงไปตรงมา แต่กติกาสิทธิ์ปัดคำสั่งลบ
ตกเงียบๆ (ลบได้ 0 แถว ไม่ error) แล้วไปตายตอน insert ว่าซ้ำ

ถ้าตรวจด้วยการอ่านโค้ดอย่างเดียวจะดูเหมือนแก้แล้ว แล้วไปพังตอนอยู่หน้าเครื่องจริง
**เรื่องที่เกี่ยวกับ RLS ต้องรันบน Postgres จริงเสมอ** ไม่ใช่แค่ให้เหตุผลว่าน่าจะได้

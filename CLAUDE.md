# Doolaekan (ดูแลกัน)

สมุดสุขภาพครอบครัว ใช้ร่วมกันหลายคน — PWA ภาษาไทย

**สิ่งสำคัญที่สุดที่ต้องเข้าใจก่อนแตะโค้ด: เจ้าของแอปกับพ่อแม่ไม่ได้อยู่บ้าน
เดียวกัน** และคนดูแลใช้เครื่องของแม่ ทุกการตัดสินใจในแอปนี้เกิดจากข้อนี้ —
ข้อมูลต้องเห็นตรงกันทุกเครื่อง ทุกคนต้องรู้ว่าใครกดอะไรไว้ และห้ามมีอะไรที่
ต้องเดินไปกดที่เครื่องอีกฝ่าย

## เป้าหมายปลายทาง

ปล่อยเป็นแอปแจกฟรีบน Play Store และ App Store (มีบัญชีนักพัฒนาทั้งสองแล้ว)
ไม่รีบ แต่ทุกการตัดสินใจไม่ควรปิดทางนี้

**ลำดับที่ตกลงกันไว้ — ห้ามข้าม**

1. ~~ย้ายไป doolaekan.com~~ **เสร็จแล้ว** — แอปอยู่ที่ https://doolaekan.com
   (`public/CNAME` อยู่ในโปรเจกต์แล้ว, custom domain ตั้งใน Settings → Pages แล้ว,
   Enforce HTTPS เปิดแล้ว) ที่อยู่เดิมบน github.io เด้งมาที่นี่ให้เอง
2. **แจ้งเตือน Web Push** (โค้ดอยู่ใน branch `claude/doolaekan-domain-push-j6ow5y`
   ยังไม่รวมเข้ามา — ตรวจแล้วและแก้ช่องโหว่ 3 จุดแล้ว พร้อมรวมเมื่อถึงเวลา)
3. **ขึ้น Play Store** ด้วย TWA ก่อน แล้วค่อย App Store

ข้อ 2 ต้องรอวันที่เจ้าของอยู่พร้อมหน้ากับพ่อแม่ เพราะการอนุญาตแจ้งเตือน
ต้องกดที่เครื่องเขาเอง

**ต้องมีก่อนเปิดให้คนนอกใช้**

- ~~ปุ่มลบบัญชีพร้อมข้อมูลทั้งหมด~~ **ทำแล้ว** อยู่ในชีตโปรไฟล์ ใต้ปุ่มออกจากระบบ
  ลบแถว `profiles` ของตัวเองแถวเดียว ที่เหลือหายตาม cascade — ไม่ต้องรัน SQL
  แต่ถ้าอยากให้ลบตัวอีเมลที่ใช้เข้าระบบด้วย ต้องรัน `0012_delete_account.sql`
  (ยังไม่ได้รัน — แอปบอกผู้ใช้ตามจริงอยู่แล้วว่าอีเมลยังอยู่)
- ~~หน้านโยบายความเป็นส่วนตัว~~ **ทำแล้ว** ที่ `/privacy` ลิงก์จากชีตโปรไฟล์
  และจากท้ายหน้าแนะนำ — ยังต้องกรอกแบบฟอร์มประกาศว่าเป็นแอปสุขภาพตอนส่งขึ้นสโตร์
- ย้ายอีเมล OTP ออกจาก Gmail ส่วนตัว (ตอนนี้ยิงผ่านบัญชีเจ้าของ จำกัดวันละ ~500
  ฉบับ และคนแปลกหน้าเห็นอีเมลเขาเป็นผู้ส่ง) — และเปลี่ยน app password ที่เคยหลุด
  ในภาพหน้าจอ
- ตัดสินใจเรื่องค่า Supabase — แพลนฟรี (5 GB egress / 500 MB DB / 1 GB ไฟล์)
  รับคนแปลกหน้าที่ถ่ายรูปเอกสารกันไม่ไหว และจะเต็มแบบเงียบๆ

## กติกาที่ห้ามฝ่าฝืน

**ห้ามใช้ `upsert` กับตาราง `books`** — `INSERT ... ON CONFLICT DO UPDATE` ทำให้
Postgres ตรวจกติกา UPDATE ด้วยเสมอ แม้แถวนั้นยังไม่มีอยู่จริง กติกา update ของ
สมุดคือ "ต้องเข้าถึงสมุดเล่มนี้ได้ระดับ full" ซึ่งเป็นไปไม่ได้สำหรับสมุดที่กำลัง
จะถูกสร้าง ผลคือสร้างสมุดใหม่ไม่ได้เลยและได้ error 42501 ที่ชี้ไปผิดทาง
กติกาเดียวกันใช้กับทุกตารางที่ policy ของ UPDATE ต่างจาก INSERT

**ห้ามเก็บ base64 ลงคอลัมน์ text ของ Postgres** — เคยกิน egress ไป 32 GB เพราะ
รูปถุงยาถูกเขียนลง `medications.photo_path` รูปต้องขึ้น Supabase Storage เท่านั้น

**รูปคือของหนักที่สุดในแอปนี้** — ย่อก่อนเก็บเสมอ และโหลดเฉพาะใบที่ผู้ใช้กดดู
ห้ามดึงรูปทุกใบตอนเปิดแอป การไม่ดึงรูปคือเหตุผลหลักที่แอปเปิดได้เร็ว
เมื่อเปิดให้คนนอกใช้ ทุกไบต์กลายเป็นเงินที่เจ้าของจ่ายแทนคนอื่น

ค่าย่อรูปอยู่ที่ `src/lib/photo.ts` ที่เดียว มีสองระดับเพราะใช้คนละงาน
- `shrinkPhoto` 1000px/0.72 — รูปอาหาร รูปในยิม มีไว้ให้จำได้ว่าทำอะไร
- `shrinkDoc` 1600px/0.8 — รูปเอกสาร ต้องอ่านตัวเลขบนใบผลเลือดออก
- PDF ย่อไม่ได้ เก็บตามเดิม และเป็นต้นฉบับจริงจากโรงพยาบาลอยู่แล้ว

**บันทึกประจำวัน (กิจกรรม/อาหาร) ใส่ได้รูปเดียว ส่วนเอกสารใส่ได้หลายแผ่น**
เพราะผลตรวจใบเดียวมักมีหลายหน้าจริงๆ การบังคับให้เหลือแผ่นเดียวไม่ได้ประหยัด
พื้นที่ — คนจะสร้างบันทึกเพิ่มแทน จำนวนรูปเท่าเดิม ได้แค่ความรำคาญ

**ห้ามทำอะไรที่บังคับให้ทุกเครื่องล็อกอินใหม่ โดยไม่ได้รับคำสั่งชัดเจน** —
เปลี่ยนโดเมนคือตัวอย่าง เคยเผลอ deploy ไปแล้วต้องรีบย้อนกลับ

**แอปต้องไม่โกหก** — นี่เป็นหลักที่ยึดมาตลอด
- ยอดแคลอรี่ที่กรอกไม่ครบ ต้องบอกว่ามาจากกี่รายการ ไม่ใช่แสดงเป็นยอดเต็ม
- "ยังไม่ได้กดกินยา" ไม่เท่ากับ "ยังไม่ได้กินยา" — อย่าเอาไปแสดงเป็นเรื่องน่ากังวล
- วันที่ฉีดวัคซีนที่จำได้แค่ปี ต้องแสดงแค่ปี ไม่เติมวันให้ดูแม่นเกินจริง
- ดึงข้อมูลไม่สำเร็จ ไม่เท่ากับ ยังไม่มีสมุด — ห้ามพาผู้ใช้ไปหน้ากรอกข้อมูลใหม่
- ข้อความ error ต้องบอกว่าพังตรงไหนจริงๆ ไม่ใช่ข้อความรวมๆ ที่ชี้ผิดทาง

**ของที่ผู้ใช้พิมพ์หรือถ่ายไว้ ห้ามหายเพราะกดพลาด** — เคยทำผู้ใช้เสียรูปเอกสาร
19 ใบเพราะปุ่มบันทึกกดไม่ได้โดยไม่บอกเหตุผล ปิดชีตต้องเก็บของไว้ ลบต้องถามก่อน
และห้ามบังคับกรอกช่องที่ไม่จำเป็นก่อนถึงจะบันทึกได้

**ผู้สูงอายุจำอะไรไม่ครบเป็นเรื่องปกติ** — ยอมให้กรอกอายุแทนวันเกิด ยอมให้ลง
วัคซีนแบบรู้แค่ปี บังคับให้กรอกเป๊ะ = คนเดามั่วหรือไม่กรอกเลย

**อย่าเขียนอะไรที่สมมติว่ามีแค่ไม่กี่คนในกลุ่มเดียว** — แก้ทีหลังแพงที่สุด

**ห้ามตรึง `position: fixed` ครอบหน้าที่มีช่องพิมพ์** — กล่องที่ตรึงยึดกับกรอบ
หน้าเว็บ ซึ่งบนมือถือไม่หดตอนคีย์บอร์ดเด้ง เบราว์เซอร์จะพยายามเลื่อนช่องที่กำลัง
พิมพ์ให้เห็นแล้วไปสู้กับกล่องที่ตรึง กลายเป็นจอกระตุกและแตะไม่โดน ผู้ใช้ใหม่สอง
คนเจออาการนี้ตอนกรอกสมุดเล่มแรก และไม่มีใครในบ้านเจอเลยเพราะผ่านหน้านั้นไปนานแล้ว
ถ้าต้องเต็มจอ ใช้ `min-height: 100dvh` แบบไหลตามปกติ หรือ `max-height: 88dvh`
แบบชีต ซึ่งหดตามคีย์บอร์ด และอย่าให้ปุ่มอะไรลอยตรึงอยู่ล่างจอในหน้าที่ต้องพิมพ์
เพราะมันจะไปเกาะขอบคีย์บอร์ดแล้วบังช่องพอดี

## โครงสร้าง

Next.js App Router + React + TypeScript strict, `output: 'export'`
ขึ้น GitHub Pages ด้วย GitHub Actions (deploy เมื่อ push เข้า branch ที่กำหนดใน
workflow) หลังบ้าน Supabase — Postgres + RLS + Realtime + Storage + OTP ทางอีเมล

- `src/lib/store.tsx` — สถานะทั้งแอปและ action ทุกตัว เขียนลงเครื่องก่อนแล้วค่อย
  ส่งขึ้นคลาวด์ ส่งไม่สำเร็จต้องขึ้นแถบเตือนค้างไว้ ไม่ใช่ toast ที่หายไปเอง
- `src/lib/remote.ts` — คุยกับ Supabase ที่เดียว ไทม์ไลน์สุขภาพกับบันทึกประจำวัน
  ดึงคนละโควตากัน ไม่งั้นการจดทุกวันจะเบียดของเก่าหายไปเงียบๆ
- `src/lib/selectors.ts` — คำนวณทุกอย่างจาก state ไม่เก็บค่าซ้ำ
- `supabase/migrations/` — รันเรียงเลข ต้องรันซ้ำได้โดยไม่พัง

**ตาราง `records` มีคอลัมน์ `data` เป็น jsonb** — ใช้เก็บของใหม่ได้โดยไม่ต้องแก้
โครงฐานข้อมูล (วัคซีนเก็บแบบนี้) เหมาะมากเวลาที่เจ้าของยังไม่สะดวกรัน SQL

## การทดสอบ

ทดสอบด้วยเบราว์เซอร์จริงเสมอ ไม่ใช่แค่ typecheck — ชุดทดสอบ Playwright อยู่ที่
`tests/` วิธีตั้งเซิร์ฟเวอร์และรายละเอียดของแต่ละสคริปต์อยู่ใน `tests/README.md`

ก่อน push ทุกครั้ง: typecheck ผ่าน + ชุดทดสอบของฟีเจอร์นั้นผ่าน + ชุดเดิมไม่พัง
ถ้าสคริปต์เดิมตก ให้พิสูจน์ก่อนว่าตกเพราะของใหม่จริงหรือตกอยู่แล้ว (build จาก
commit ก่อนหน้าแล้วรันเทียบ) — `tests/nobook.js` ตกเป็นครั้งคราวประมาณ 1 ใน 3
มาแต่ไหนแต่ไร ห้ามใช้ผลตกของตัวนี้ตัดสินอะไร

## เรื่องที่ยังไม่เคยทดสอบ

- ระดับแชร์ **"เฉพาะวันนัด"** — ไม่เคยมีใครใช้จริงในแอป (แต่โค้ดแจ้งเตือน
  ทดสอบระดับนี้ไว้แล้วบน Postgres จริง)
- โค้ดแจ้งเตือนทั้งชุด ยังไม่เคยรันบนฐานข้อมูลจริงของโปรเจกต์

## วันที่ย้ายโดเมน — บทเรียนที่แพงที่สุด

ย้ายไป doolaekan.com ใช้เวลาทั้งวันทั้งที่ควรจบใน 15 นาที ต้นเหตุคือสูตรบรรทัดเดียว
ใน workflow ที่เขียนไว้ก่อนหน้า

    NEXT_PUBLIC_BASE_PATH: ${{ env.CUSTOM_DOMAIN != '' && '' || format(...) }}

เป็นกับดักของ GitHub Actions ที่เขียนเลียนแบบสามทาง — ค่ากลางเป็นค่าว่างซึ่งนับ
เป็นเท็จ สูตรจึงตกไปใช้ค่าหลังสุดเสมอ **ไม่เคยคืนค่าว่างเลยแม้แต่ครั้งเดียว**
ทุก build ที่มี CNAME จึงยังชี้ไป `/doolaekan/_next/...` ขณะที่ไฟล์อยู่ที่รากโดเมน
ไฟล์ตอบ 404 หมด React ไม่เริ่มทำงาน

**อาการหลอกตามาก** หน้าเว็บที่ค้างคือ splash ที่ถูกสร้างไว้ล่วงหน้าในไฟล์ ไม่ใช่
แอปที่กำลังโหลด และเพราะ React ไม่เคยทำงาน ตัวจับเวลา 6 วินาทีที่จะเปลี่ยนข้อความ
เป็น "ใช้เวลานานกว่าปกติ" ก็ไม่เคยเริ่ม จึงดูเหมือนแค่กำลังโหลดอยู่ ไม่มี error เลย

**บทเรียน**
- ค่าที่ผิดถูกพิมพ์อยู่ในล็อกของ build ตั้งแต่รอบแรก แต่ไม่ได้เปิดดู เพราะเชื่อ
  คอมเมนต์ที่เขียนกำกับไว้ข้างสูตร **คอมเมนต์ไม่ใช่หลักฐาน ล็อกคือหลักฐาน**
- เวลาเว็บพัง ให้ดู Console ของเบราว์เซอร์เป็นอย่างแรก ไม่ใช่อย่างสุดท้าย
  ที่อยู่ของไฟล์ที่ 404 บอกคำตอบทั้งหมดในบรรทัดเดียว
- ห้ามใช้ `A && '' || B` ใน GitHub Actions เด็ดขาด คำนวณด้วย shell แล้วเขียนลง
  `$GITHUB_ENV` แทน
- ตอนนี้มีด่านตรวจใน workflow แล้ว ถ้า index.html ชี้ไฟล์ไม่ตรงกับ basePath
  build จะพังทันทีและไม่ deploy — ห้ามถอดออก

**เรื่องเบราว์เซอร์ที่ต้องรู้ตอนย้ายโดเมน**
- ตอนผูก custom domain GitHub สั่งเบราว์เซอร์เด้งจาก github.io ไปโดเมนใหม่
  แบบถาวร และ **เบราว์เซอร์จำไว้แม้จะลบ custom domain ออกแล้ว** ถอยกลับที่อยู่เดิม
  จึงไม่ได้ผลกับเครื่องที่เคยเปิด นอกจากล้างข้อมูลเว็บไซต์ทีละเครื่อง
- service worker ที่เก็บหน้าเก่าไว้ทำให้ติดกับดัก: โค้ดที่อัปเดต service worker
  อยู่ในตัวแอป พอแอปเริ่มทำงานไม่ได้ ตัวเก่าก็ค้างตลอด ต้องล้างด้วยมือ
  **ยังไม่ได้แก้ — ควรใส่ตัวกู้ตัวเองที่ล้าง sw แล้วโหลดใหม่ถ้าแอปไม่เริ่มใน 8 วินาที**
- อย่าถอยกลับกลางทางถ้ายังไม่รู้สาเหตุ การ build สลับไปมาสองแบบในชั่วโมงเดียว
  ทำให้แยกไม่ออกว่าอะไรเป็นผลของอะไร และทำให้เสียเวลาไปหลายชั่วโมง

## บทเรียนเรื่องการตรวจโค้ด

ตอนแก้ช่องโหว่ในโค้ดแจ้งเตือน วิธีแก้แรกที่ "อ่านแล้วดูถูก" ใช้ไม่ได้จริง —
ลบแถวของบัญชีอื่นก่อนแล้วค่อยเพิ่มใหม่ ฟังดูตรงไปตรงมา แต่กติกาสิทธิ์ปัดคำสั่งลบ
ตกเงียบๆ (ลบได้ 0 แถว ไม่ error) แล้วไปตายตอน insert ว่าซ้ำ

ถ้าตรวจด้วยการอ่านโค้ดอย่างเดียวจะดูเหมือนแก้แล้ว แล้วไปพังตอนอยู่หน้าเครื่องจริง
**เรื่องที่เกี่ยวกับ RLS ต้องรันบน Postgres จริงเสมอ** ไม่ใช่แค่ให้เหตุผลว่าน่าจะได้
